# StreamMerge-In-Place-Ordered-Data-Pipeline
StreamMerge is a lightweight JavaScript engine for combining two pre-sorted data streams into one ordered sequence without extra storage. It uses a backward two-pointer strategy to preserve existing data while merging efficiently, demonstrating in-place processing, deterministic ordering, and O(n) performance. at scale
